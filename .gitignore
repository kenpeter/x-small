# checkpoints NEVER in git (278MB each — user rule: ckpts live in project dir on disk only)
checkpoints/
*.pt
*.pkl

# scratch / experiments
runs/
probe_mem*.py
__pycache__/
*.pyc

# curriculum auto-backups + training logs
curriculum_boost.json.bak-*
run_*.log
smoke*.log

# eval outputs (large, regenerate locally)
evaluations/

# local logs
logs/

# python venvs (never commit)
venv*/
.venv/